# 2. Реализовать проект расчета суммарного расхода ткани на производство одежды.
# Основная сущность (класс) этого проекта - одежда, которая может иметь определенное название.
# К типу одежды в этом закрытом пальто и костюм.
# У этих типов одежды существуют: размер (для пальто) и рост (для костюма).
# Это могут быть обычные числа: V и H, соответственно.
# Для определения расхода ткани по каждому типу одежды использовать формулы: для пальто (V / 6.5 + 0.5),
# для костюма (2 * H + 0,3). Проверить работу этих методов на реальных данных.
# Реализовать общий подсчет расхода ткани.
# Проверить на практике полученные на этом уроке знания: реализовать абстрактные классы для основных классов проекта,
# проверить на практике работу декоратора @property.

from  abc  import  ABC , abstractmethod


класс  Одежда ( ABC ): # одежда
    имя : ул.

    def  __init__ ( я , имя : str ):
        я . name  =  имя

    @ свойство
    @ abstractmethod
    def  вычислить ( самостоятельно ):
        проходят


class  Coat ( Одежда ): # пальто
    _size : float

    def  __init__ ( self , name : str , size : float ):
        супер (). __init__ ( имя )
        я . _size  =  размер

    @ свойство
    def  вычислить ( самостоятельно ):
        возвратный  раунд (( self . _size  /  6.5  +  0.5 ), 2 )


class  Suit ( Одежда ): # костюм
    _height : float

    def  __init__ ( self , name : str , height : float ):
        супер (). __init__ ( имя )
        я . _height  =  высота

    @ свойство
    def  вычислить ( самостоятельно ):
        возвратный  раунд (( 2  *  self . _height  +  0.3 ), 2 )


пальто  =  Пальто ( 'Пальто' , 6.2 )
print ( f "На пошив { пальто . имя } потребуется { пальто . вычислить } метров ткани." )

suit  =  Костюм ( 'Костюм' , 0.51 )
print ( f "На пошив { костюм . имя } а потребуется { костюм . вычислить } метров ткани." )
