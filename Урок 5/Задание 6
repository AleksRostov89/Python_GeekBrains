# 6. Необходимо создать (не программно) текстовый файл, где каждая конкретный учебный предмет
# и наличие лекционных, практических и лабораторных занятий по этому предмету и их количество.
# Важно, чтобы для каждого предмета были обязательно все типы занятий.
# Сформировать словарь, разделить название предмета и общее количество занятий по нему.
# Вывести словарь на экран.
# Примеры строк файла: Информатика: 100 (л) 50 (пр) 20 (лаб).
# Физика: 30 (л) - 10 (лаб)
# Физкультура: - 30 (пр) -
# Пример словаря: {«Информатика»: 170, «Физика»: 40, «Физкультура»: 30}

с  open ( 'task06.txt' , encoding = 'UTF-8' ) as  my_files :   # менеджер контекста - открываем на чтение
    my_dict  = {}   # создаем пустой словарь
    для  строки  в  my_files :   # цикл, перебираем строки из файла
        строка  =  строка . replace ( "(л)" , "" )   # избавляемся от не нужных символов в строке
        строка  =  строка . replace ( "(пр)" , "" )
        строка  =  строка . replace ( "(лаб)" , "" )
        строка  =  строка . replace ( "-" , "" )
        строка  =  строка . replace ( ":" , "" )
        word_row  =  строка . split ()   # разбиваем строку на слова
        sum_row  =  0   # переменная, куда записывается сумма часов по предмету
        for  word  в  word_row [ 1 :]:   # цикл, перебираем значения начиная со второго и до конца
            sum_row  + =  int ( word )   # суммируем часы по предмету
            my_dict . update ({ word_row [ 0 ]: sum_row })   # записываем в словарь значения
    print ( f "Словарь: { my_dict } " )   # вывод словаря
