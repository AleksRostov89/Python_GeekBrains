# 1. Создать класс TrafficLight (светофор) и определить у него один атрибут color (цвет) и метод running (запуск).
# Атрибут реализовать как приватный.
# В методе реализации переключения светофора в режимы: красный, желтый, зеленый.
# Продолжительность первого состояния (красный) составляет 7 секунд, второго (желтый) - 2 секунды,
# третьего (зеленый) - на ваше усмотрение.
# Переключение между режимами должно выполняться только в указанном порядке (красный, желтый, зеленый).
# Проверить работу примера, создаваемого экземпляра и вызвав описанный метод.
 время импорта


class  TrafficLight :   # класс
    __color : str   # приватный атрибут класса

    def  running ( self ):   # метод
        я . __color  =  "Красный"
        print ( f "Сигнал светофора: { self . __color } " )
        время . сон ( 7 )

        я . __color  =  «Желтый»
        print ( f "Сигнал светофора: { self . __color } " )
        время . сон ( 2 )

        я . __color  =  "Зеленый"
        print ( f "Сигнал светофора: { self . __color } " )
        время . сон ( 3 )


traffic_light  =  TrafficLight ()   # экземпляр класса
traffic_light . running ()   # вызов метода
